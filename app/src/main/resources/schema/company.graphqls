input CreateCompanyInput {
    name: String!
}

type CompanyPayload {
    id: ID!
    name: String!
    departments: [DepartmentPayload]
    businessPartners: [BusinessPartnerPayload]
    paymentTerms: [PaymentTermPayload]
    taxes: [TaxPayload]
}

extend type Mutation {
    createCompany(input: CreateCompanyInput!): CompanyPayload! @auth(permissions: ["GENERAL:ADMIN"])
}

extend type Query {
    company(id: ID!): CompanyPayload
    companies(tenantId: ID!): [CompanyPayload]
}