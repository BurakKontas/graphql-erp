-- Create schema for tenant (MySQL)
-- In MySQL, schema and database are synonyms. Use CREATE DATABASE if necessary.
-- We will create the schema as a database; placeholder ${schema}
CREATE DATABASE IF NOT EXISTS `${schema}`;
